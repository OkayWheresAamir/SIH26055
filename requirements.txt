# SIH26055 — direct dependencies only. Transitive ones are left to pip.
#
#     python -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Lower bounds, not pins: nothing here is version-sensitive, and the versions
# below are what the suite was last run against (2026-09-04, 110 tests passing).
# The freeze list is `rfenv/constants.py`, not this file — no result depends on
# a library version.

# The environment (rfenv/), L0–L3.
numpy>=2.0            # 2.5.2
h5py>=3.10            # 3.16.0 — reading the Turing HDF5 recordings
gymnasium>=1.0        # 1.3.0  — the L3 agent interface

# Artefacts. matplotlib is imported by `rfenv/render/` and by nothing else, so
# the environment, its metrics and the whole test suite bar tests/test_render.py
# run without a plotting stack installed.
matplotlib>=3.8       # 3.11.1

# docsearch/. Lazily imported in `docsearch/corpus.py` — only needed to rebuild
# the index (`python -m docsearch.corpus --rebuild`), not to query it.
pymupdf>=1.24         # 1.28.2

# rfenv/rl/ — rungs 7 (DQN) and 8 (PPO), EVALUATION.md §5. Imported by
# tests/test_rl.py and lazily by rfenv/baselines/ladder.py's rung 7/8 factories
# only, so the rest of the suite runs without a training stack installed. SB3
# has historically pinned an upper bound on gymnasium, and this project runs
# gymnasium==1.3.0 — confirmed compatible below (step 1 of verification).
torch>=2.3             # 2.14.0
stable-baselines3>=2.3 # 2.9.0

# rfenv/rl/recurrent_ppo.py — rung 9, an LSTM-recurrent PPO variant. Plain SB3
# has no recurrent policy at all (MlpPolicy is feed-forward only), which is
# what sb3-contrib adds: RecurrentPPO + MlpLstmPolicy, same .learn()/.predict()
# shape otherwise. Pinned to the same minor version as stable-baselines3 above
# since sb3-contrib tracks it release-for-release.
sb3-contrib>=2.3       # 2.9.0

# Tests.
pytest>=8.0           # 9.1.1

# Handoff documents. Only needed by `scripts/md2pdf.py`, which renders the
# markdown handoffs in docs/project/ to PDF. Pure Python — no browser, no system
# libraries. Nothing in rfenv/ or tests/ imports either.
markdown>=3.6         # 3.10.3
xhtml2pdf>=0.2.17     # 0.2.18 — ReportLab underneath
